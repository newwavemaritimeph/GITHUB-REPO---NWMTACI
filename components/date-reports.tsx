"use client";
import {useState} from "react";
export function DateReports(){const today=new Date().toISOString().slice(0,10),month=`${today.slice(0,8)}01`,[from,setFrom]=useState(month),[to,setTo]=useState(today),[report,setReport]=useState("payments");const url=`/api/reports/operations?report=${report}&from=${from}&to=${to}`;return <div className="portal-panel date-report-card"><div><h2>Date-sensitive report export</h2><p>Every export is filtered by the selected inclusive date range and recorded in the audit log.</p></div><label>Report<select value={report} onChange={e=>setReport(e.target.value)}><option value="payments">Payments and collections</option><option value="enrollments">Enrollments and rebate snapshots</option><option value="expenses">Expenses</option><option value="attendance">Attendance events</option></select></label><label>From<input type="date" value={from} onChange={e=>setFrom(e.target.value)}/></label><label>To<input type="date" value={to} onChange={e=>setTo(e.target.value)}/></label><a className="portal-primary" href={url}>Download XLSX</a></div>}
